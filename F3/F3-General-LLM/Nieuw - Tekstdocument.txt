# ========================================================== # F3 Regulations RAG System - CPU Optimized, GGUF 8-bit # ========================================================== 
import ast 
import os from bs4 
import BeautifulSoup 
from pypdf import PdfReader 
from ollama import chat import re 
from pywebio.input import input, TEXT 
from pywebio.output 
import put_text, use_scope, put_table, put_markdown, put_file, put_loading from pathlib 
import Path 
# ========================================================== # 1️⃣ Load PDFs as full text # ========================================================== 
pdf_files = { "2025": "regulations_f3_2025.pdf", "2026": "regulations_f3_2026.pdf" } 
documents = {} print("Loading PDFs...") 
for year, filename in pdf_files.items(): 
	print(f"Processing {filename}...") 
reader = PdfReader(filename) text = "" for page in reader.pages: page_text = page.extract_text() if page_text: text += page_text + "\n" documents[year] = text print(f" PDF loaded for {year}, length: {len(text)} characters") pattern = r'^(?:ARTICLE\s+\d+.*|\d+(?:\.\d+)+.*)' headings_2025 = re.findall(pattern, documents["2025"], re.MULTILINE) headings_2026 = re.findall(pattern, documents["2026"], re.MULTILINE) # ========================================================== # 2️⃣ Load question from email # ========================================================== import sys # ========================================================== # 2️⃣ Load question (HTML OR terminal) # ========================================================== def get_user_question(prompt_text="Type your question:"): """ Prompt the user for a question directly. Returns the entered question as a string. """ question = "Compare all sections" if not question: raise ValueError("No question provided.") return question # Example usage user_question = get_user_question("Enter the question about F3 regulations: ") print(f"\nQuestion: {user_question[:100]}...\n") # ========================================================== # 3️⃣ Reason about relevant sections based on question # ========================================================== reason_prompt = f""" You are analyzing the Formula 3 technical regulations changes from 2025 and 2026. Question: {user_question} Available Headings (2026): {headings_2026} Task: Identify the relevant section headings from the list above that directly help answer the question. Rules: - Only select headings from the 'Available Headings (2026)' list. - Each heading must exactly match an item in the list; do NOT modify or paraphrase. - Do NOT add explanations, notes, or any text beyond the headings. - Output must be a valid Python list of strings. - Do not include any text before or after the list. - Do not use f-string braces or any other Python syntax outside the list. Example of correct output: ["Heading 1", "Heading 2"] """ # ========================================================== # 4️⃣ Load GGUF 8-bit model # ========================================================== GGUF_MODEL_PATH = r"C:\Users\ArjanFaberVAR\OneDrive - Van Amersfoort Racing B.V\Regulations-VAR - Documenten\Meta-Llama-3-8B-Instruct.Q4_0.gguf" n_threads = os.cpu_count() - 1 import subprocess import time # Start Ollama server OLLAMA_EXE = r"C:\Users\ArjanFaberVAR\AppData\Local\Programs\Ollama\Ollama.exe" MODEL_NAME = "minimax-m2.7:cloud" API_KEY = "ca10255cdb524f099a1f4e5368f6e15e.wLRsBmrD5Xed0ZaDGNPNun1G" os.environ["OLLAMA_API_KEY"] = API_KEY # 1️⃣ Pull the model if it doesn't exist try: # List existing models result = subprocess.run([OLLAMA_EXE, "list"], capture_output=True, text=True) if MODEL_NAME not in result.stdout: print(f"Model '{MODEL_NAME}' not found. Pulling it now...") subprocess.run([OLLAMA_EXE, "pull", MODEL_NAME], check=True) else: print(f"Model '{MODEL_NAME}' is already downloaded.") except Exception as e: print("Error checking/pulling Ollama model:", e) raise # 2️⃣ Start Ollama server (if not already running) # Comment this out if you already have the server running subprocess.Popen([OLLAMA_EXE, "serve"]) # 3️⃣ Give it a few seconds to start time.sleep(5) print("Ollama server should be ready to accept requests.") def ask_ollama(prompt, system=None, max_tokens=800, temperature=0.0): messages = [] if system: messages.append({"role": "system", "content": system}) messages.append({"role": "user", "content": prompt}) response = chat( model=MODEL_NAME, messages=messages, options={ "temperature": temperature, "num_predict": max_tokens } ) return response["message"]["content"] # Ask the model relevant_sections_text = ask_ollama(reason_prompt, max_tokens=800, temperature=0.0) # Split output raw_sections = re.split(r'[,\n]', relevant_sections_text) # Parse AI output safely sections_to_include = [] try: # Convert stringified list to actual Python list parsed_sections = ast.literal_eval(relevant_sections_text) if isinstance(parsed_sections, list): # Strip whitespace and remove empty items sections_to_include = [s.strip() for s in parsed_sections if s.strip()] except Exception as e: print("⚠ Could not parse AI output as list:", e) # Fallback if nothing valid if not sections_to_include: print("⚠ No valid sections suggested by AI, comparing entire documents") sections_to_include = ["__FULL_DOC__"] print("Sections to include for comparison:", sections_to_include) # ========================================================== # 5️⃣ System prompt # ========================================================== system_prompt = """ You are scanning and comparing two regulation texts: one from 2025 and one from 2026. Strict rules: 1. Comparison Scope: - If needed, compare **only** the 2025 text against the 2026 text. - Otherwise, provide answers based solely on the context of the text in question. 2. Show Only Changes: - Display **only the wording that changed** between the two years. - Do not repeat lines or sections that are identical. 3. Clear Year Labels: - Use this format for each change: 2025: [text from 2025] 2026: [text from 2026] - If a line was **added** in 2026, show it only under 2026. - If a line was **removed** in 2026, show it only under 2025. 4. Formatting: - Maintain a **clean, readable format**; avoid any extraneous symbols, line numbers, or Git diff metadata. - Do not include explanations, comments, or analysis — just the text changes. 5. Identical Lines: - If the line or section is identical in both years, do **not** include it. 6. Edge Cases: - If a line was modified slightly (e.g., wording change, punctuation, or numeric value), show the old version under 2025 and the new version under 2026. - Preserve the exact wording, including punctuation, capitalization, and spacing. - If context is required to understand a change, provide only what is necessary to show the change clearly, without adding extra text. 7. Output Only: - Only provide the changed lines as specified. Do not summarize, interpret, or comment on them. """ # ========================================================== # 6️⃣ Helper: extract section text & split long text # ========================================================== def extract_section_text(full_text, section_name): """ Extracts the text of a section by looking for its heading and stopping at the next heading (Article, Section, etc.) or end of document. """ if section_name == "__FULL_DOC__": return full_text pattern = rf'({re.escape(section_name)}.*?)(?=^\s*(Article|Section)\s+\d+|\Z)' match = re.search(pattern, full_text, flags=re.IGNORECASE | re.MULTILINE | re.DOTALL) return match.group(1).strip() if match else "" def split_text(text, max_words=1200): """ Splits text into chunks to avoid exceeding model context """ words = text.split() return [" ".join(words[i:i+max_words]) for i in range(0, len(words), max_words)] # ========================================================== # 7️⃣ Generate answers per section/chunk # ========================================================== answers = [] # Calculate total chunks to process total_chunks = 0 section_chunks_info = {} # optional: store per section for debug for section in sections_to_include: text_2025 = extract_section_text(documents["2025"], section) text_2026 = extract_section_text(documents["2026"], section) if not text_2025 or not text_2026: print(f"⚠ Section '{section}' missing in one of the documents") continue chunks_2025 = split_text(text_2025) chunks_2026 = split_text(text_2026) num_chunks = min(len(chunks_2025), len(chunks_2026)) total_chunks += num_chunks section_chunks_info[section] = num_chunks print(f"\nTotal chunks to process: {total_chunks}") for sec, num in section_chunks_info.items(): print(f" Section '{sec}': {num} chunk(s)") print("\nStarting processing...\n") # Now the original loop for section in sections_to_include: text_2025 = extract_section_text(documents["2025"], section) text_2026 = extract_section_text(documents["2026"], section) if not text_2025 or not text_2026: continue chunks_2025 = split_text(text_2025) chunks_2026 = split_text(text_2026) for i, (c1, c2) in enumerate(zip(chunks_2025, chunks_2026), start=1): prompt = f"""{system_prompt} Section comparison: {section} (chunk {i}) ----- 2025 ----- {c1} ----- 2026 ----- {c2} Answer:""" answer_text = ask_ollama(prompt, system=system_prompt, max_tokens=4500) answers.append(f"Section {section} chunk {i}:\n{answer_text}\n") print(f"Section '{section}' chunk {i} processed, answer length: {len(answer_text)} characters") # ========================================================== # 8️⃣ Merge and save answers # ========================================================== final_answer = "\n".join(answers) system_prompt = f""" You are reviewing a comparison of Formula 3 technical regulations between 2025 and 2026. Strict rules: 1. Comparison Scope: - Compare ONLY the 2025 text against the 2026 text. - Do not introduce external knowledge. 2. Question Relevance: - Include ONLY changes directly relevant to the question: "{user_question}" - Exclude unrelated changes completely. 3. Output Structure: - Organize the answer into clear sections using headings: ## [Section Name] - Within each section, group related changes logically. 4. Change Format: - For every change, use EXACTLY this format: 2025: [original text, including article number if present] 2026: [updated text, including article number if present] - Rules: - If text is added → show ONLY under 2026 - If text is removed → show ONLY under 2025 - If modified → show both versions - Always include the article numbers of the regulations alongside the text. 5. Formatting Requirements: - Preserve original wording EXACTLY (no paraphrasing) - Keep numbering, bullets, and formatting intact - Ensure readability (line breaks between changes) 6. Do NOT include: - Identical text - Explanations inside the comparison - Commentary between changes 7. Summary Section (MANDATORY): - After all changes, include: ## Summary of Key Changes - Provide a concise, high-level explanation of: - What changed - Why it matters (engineering / compliance impact) - Include references to all relevant article numbers - Use reasoning and interpretation - Do NOT copy text from above 8. Output Only: - Do not include any text outside the defined structure """ idx = 0 final_answer = ask_ollama(final_answer, system=system_prompt, max_tokens=4500) ANSWER_FILE = f'./answers/first_answer_{idx}.txt' os.makedirs(os.path.dirname(ANSWER_FILE), exist_ok=True) with open(ANSWER_FILE, "w", encoding="utf-8") as f: f.write(final_answer) print(f"Answer saved to {ANSWER_FILE}") answer_counter = 0 variable = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J'] from pywebio import start_server from pywebio.input import input, TEXT from pywebio.output import * import os from pathlib import Path def chat_app(): answer_counter = 0 final_answer = "" system_prompt = "" variable = [f"q{i}" for i in range(10)] while answer_counter < 10: user_question = input("Enter your question (or type 'exit' to quit):", type=TEXT).strip() if user_question.lower() == "exit": put_text("Exiting chat. Goodbye!") break if not user_question: put_text("⚠ No question entered. Please try again.") continue with use_scope(variable[answer_counter], clear=True): put_text(user_question).style('color: #27ae60') try: with put_loading(shape='border', color='light'): put_text("Ollama model is thinking...") if final_answer: system_prompt += "\n" + final_answer final_answer = ask_ollama( user_question, system=system_prompt, max_tokens=4500 ) put_text(final_answer) # Save per session os.makedirs("./answers", exist_ok=True) answer_file = f"./answers/answer_{answer_counter}.txt" with open(answer_file, "w", encoding="utf-8") as f: f.write(final_answer) except Exception as e: put_text(f"⚠ Error during Ollama request: {e}") answer_counter += 1 # Show saved files folder = Path("./answers/") files = list(folder.glob("*.txt")) rows = [['Type', 'Content']] for file in files: rows.append([ 'file', put_file(file.name, open(file, 'rb').read()) ]) put_table(rows) start_server(chat_app, port=8080, debug=True)